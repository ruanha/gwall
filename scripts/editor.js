"use strict"
/* CONSTANTS */


/* APP EVENTLISTENERS  */

/*
background.addEventListener('click', (e)=>{

	const choices = { "insert background":["stock-photo"],
					"insert frame":["15x20", "20x30", "30x40"] }
	const events = { "stock-photo":[choiceStockBackground], "15x20":[frameChoice, 60, 80], 
				"20x30":[frameChoice, 80, 120], "30x40":[frameChoice, 120, 160] }
	let menu = MenuMouse(e, choices, events)
	console.log(menu)
	menu.insertMenu(100, 100)
	//openMenu(wallMouseMenu, [choices, events])
})
*/
/*
function openMenu(call, params){
	let overlay = document.createElement("div")
	overlay.setAttribute("id", "overlay")
	wall.appendChild(overlay)
	if (params){
		let menu = call(...params)
	}
	else{
		let menu = call()
	}
	overlay.appendChild(menu)
}
function closeMenu(){

}
*/